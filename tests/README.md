# Tests

Ce dossier accueille les suites transverses du boilerplate.

- `unit/` pour les tests unitaires purs
- `integration/` pour les tests DI, repositories, auth et flows de features
- `e2e/` pour les parcours complets
